books-per-page-count: 10

spring:
  datasource:
    url: jdbc:h2:mem:library
    initialization-mode: always
    sql-script-encoding: UTF-8
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    ROOT: DEBUG